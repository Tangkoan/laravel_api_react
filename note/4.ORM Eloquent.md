- Model | ORM Eloquent
ORM = Object Relational Mapping គឺវាយក Class <---> DB Eloquent

មាន -m គឺបានន័យថា provide Migrateion (បង្កើត File ក្នុង Floder Migrrateion )
> php artisan make:model Role -m 

Command ខាងលើគឺស្នើហ្នឹង Command ពីរ Line ខាងក្រោម
> php artisan make:model Role
> php artisan make:model Role --migration

File ដែលត្រូវមានក្នុង Table Role រួមមាន
- id
- name
- code
- description
- status
- created_at (Laravel និងផ្ដល់ Field នេះ Auto)
- updated_at (Laravel និងផ្ដល់ Field នេះ Auto)


យើងបានឃើញថាយើងបានមាន Field ខាងលើហើយប៉ុន្ដែស្រាប់តែយើងនឹកឃើញថាអត់ទេខុស Field ណាមួយ ឬខ្វះ Field ណាមួយ
role មាន s ព្រោះ table name គេមាន s
> php artisan make:migration add_test_colunm_table_roles --table=roles

យើងសរសេរ Field លើសហើយចង់លុប
> php artisan make:migration remove_test_column_from_roles_table --table=roles
            public function up(): void
            {
                Schema::table('roles', function (Blueprint $table) {
                    // លុប field ឈ្មោះ test_colunm ចោល
                    $table->dropColumn('test');
                });
            }
            public function down(): void
            {
                Schema::table('roles', function (Blueprint $table) {
                    // បង្កើតវាមកវិញ ករណីយើងចង់ rollback (ការពារការបាត់បង់ទិន្នន័យ)
                    $table->string('test')->nullable();
                });
            }
កែកូដហើយរួចត្រូវ Run command ខាងក្រោម
> php artisan migrate:fresh



Command ខាងក្រោមដើម្បីពិនិត្យមើលថាយើងមិនទាន់ migrate file ណា
> php artisan migrate:status
