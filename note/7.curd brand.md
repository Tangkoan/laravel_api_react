ក្នុងមេរៀន បង្កើត API CURD នេះគឺមាន File Image


Step by step

1. File Storage Configuration
    1. Ensure your application is set up to hadle file upload.
        in the .env file, set the FILESYSTEM_DISK to public:
        FILESYSTEM_DISK = public
    2. Run the command to create a symbolic link for public storage
        > php artisan storage:link
        This command will make the storage/add/public directory accessible from the public/storage URL,

2. // Database/ migrations/xxx_xxx_xxx_create_brands_table.php
- id
- name
- code ->unique();
- from_country
- image
- $table->enum('status', ['active', 'disble'])->defalut('active');
