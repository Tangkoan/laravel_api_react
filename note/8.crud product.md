មេរៀននេះនឹងនិយាយពីការបង្កើត Product API ដែលមាន Realationship ជាមួយ Table Brand and Category

- Table Product (Product ច្រើនក្នុង Categoryតែមួយ)
- Table Category (Category មួយគឺមាន Productច្រើន )
- Table Brand (Brand មួយគឺមាន Product ច្រើន )

1. ការបង្កើត  Model និង Migrate
> php artisan make:model Product -m
> php artisan make:controller api/ProductController --api
    - ចូលទៅកាន់ Migrate
        public function up(): void
        {
            Schema::create('products', function (Blueprint $table) {
                $table->id();
                $table->unsignedBigInteger('category_id');
                $table->unsignedBigInteger('brand_id');
                $table->string("product_name");
                $table->text("description")->nullable();
                $table->integer("quantity");
                $table->decimal("price", 10, 2);
                $table->string("image")->nullable();
                $table->boolean("status")->defalut(1);
                $table->timestamps();

                $table->foreign('category_id')
                    ->references('id')
                    ->on('categories')
                    ->onDelete('cascade');

                $table->foreign('brand_id')
                    ->references('id')
                    ->on('brands')
                    ->onDelete('cascade');
            });
        }

2. ចង់ Relationship រវាង Table ទាំងបី (Categories, Products, Brands)

+ Modal Category
    // One Category has many Product
    public function product(){
        return $this->hasMany(Product::class);
    }

+ Modal Brand
    // One Brand has many Product
    public function product(){
        return $this->hasMany(Product::class);
    }

+ Modal Product
    // One Product belongs to one Category
    public function category(){
        return $this->belognsTo(Category::class);
    }

    // One Product belongs to one Brand
    public function brand(){
        return $this->belognsTo(Brand::class);
    }